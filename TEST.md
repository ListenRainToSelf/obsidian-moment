# 此刻 MOMENT · 测试文档

> 给使用者 / QA 的验收清单。请逐项在真实 Obsidian 环境中测试并勾选。
> 括号内标注：🖐 = 人工交互验收　🔍 = 需要查看生成的文件

---

## 0. 环境准备

- [ ] 构建：在工程根目录执行 `npm install` 与 `npm run build`，确认 `main.js` 生成。
- [ ] 安装：将 `main.js` / `manifest.json` / `styles.css` 复制到 `你的库/.obsidian/plugins/obsidian-moment/`。
- [ ] 启用：Obsidian「设置 → 第三方插件 → 此刻 MOMENT」启用。
- [ ] 配置：在「设置 → 此刻 MOMENT」设置「内容根目录」，如 `日记`（库内相对路径）。

---

## 0.5 外观 · 主题（v0.2.0 新增）🖐

### 背景类型
- [ ] 设置「背景类型 = 本地图片」：把图片放附件目录，填对「背景文件名」，封面显示该图。
- [ ] 设置「背景类型 = 纯色」：封面显示所选纯色。
- [ ] 设置「背景类型 = 渐变」：封面显示所选两色的渐变。
- [ ] 设置「背景类型 = 网络URL」：填入图片直链，封面显示网络图。
- [ ] 以上改动**即时生效**，无需重载即可看到。

### 文字颜色
- [ ] 「文字颜色」留空 = 跟随主题；填入色值（如 `#1a1f2e`）后正文/卡片文字变色。

### 背景高度段（v0.2.1 新增）🖐
- [ ] 设置「背景图片高度段」共 7 档（顶→底），封面随之锚定到对应段。
- [ ] 鼠标移到封面 → 封面高度撑开整图展开；移开恢复。
- [ ] 更换背景类型（本地图 / 网络 URL）后，高度段设置同样生效。

### 心情统计（悬停显示）🖐
- [ ] 平时封面下方不显示统计，只在鼠标悬停封面时出现。
- [ ] 悬停后，底部渐变模糊层从下向上滑出「最近心情 · N 次记录」。
- [ ] 每行一个心情：名称 + 次数 + 占比%，并有渐变进度条。
- [ ] 多发布几条不同心情的动态后刷新，统计数字随之变化。
- [ ] 最近 30 天没有任何带心情的动态时，悬停只显示「心情统计」占位。

### 移除点击查看 🖐
- [ ] 点击封面背景不再弹出全屏灯箱；改为鼠标悬停封面查看整图。

### 心情候选（v0.2.1 新增）🖐
- [ ] 设置面板「心情候选」列出默认心情，可修改、删除任一心情。
- [ ] 点「＋ 添加心情」新增一行，回车保存。
- [ ] 发布浮层中的心情按钮与设置中的心情候选一致。

### 信息流滚轮 & 回到顶部（v0.2.1 修复）🖐
- [ ] 鼠标悬停在下方日常（信息流）条目上时，滚动滚轮直接整页上下滚动，而不是列表单独滚动。
- [ ] 日常（信息流）区域不出现独立滚动条，动态条目完整连续往下排布。
- [ ] 向下滚动超过一定距离后，视口右下角淡入「回到顶部」圆形按钮。
- [ ] 回到按钮浮于页面最上层，悬停时变为渐变蓝紫、白字箭头。
- [ ] 点击「回到顶部」按钮，页面平滑滚回顶部，按钮随之隐藏。
- [ ] 打开全览视图时，「回到顶部」按钮不显示（避免遮挡全览）。

### 信息流多天回溯（v0.2.1 修复）🖐
- [ ] 信息流不再只显示今天的动态，能看到最近 30 天内的历史动态。
- [ ] 动态按日期分组，每组上方有「今天 / 昨天 / 前天 / M月D日」胶囊头与条数。
- [ ] 同一天内多条动态最新在上；不同日期之间新日期在上、旧日期在下。
- [ ] 有动态的空当天自动跳过；整个 30 天范围内都没有内容时显示占位文案。
- [ ] 新发布一条动态后刷新，能立刻在本组头部看到并累计条数。

### 动态图片预览 & 排列模式（v0.2.2 新增）🖐
- [ ] 单击信息流里的任意一张图片，弹出全屏灯箱预览大图。
- [ ] 灯箱内右下角显示「x / N」张数；有多张时左右出现切换圆按钮。
- [ ] 多张时按键盘 ← / → 或点按钮可切换上一张 / 下一张。
- [ ] 点击灯箱背景或按 Esc 关闭预览。
- [ ] 设置「图片排列 = 九宫格」：带图动态按 3 列正方形缩略排列（微信风格）。
- [ ] 九宫格下 9 张以内全部显示；超过 9 张时最后一张叠加「+N」暗色小角标。
- [ ] 点击「+N」格可进入灯箱浏览后续图片。
- [ ] 设置「图片排列 = 卡片堆叠」：图片变成大图卡片竖向堆叠，一图一卡。
- [ ] 两种模式间切换保存后，信息流立刻按新模式重绘，无需重载插件。

---

## 1. 内核闭环（阶段一）

### 1.1 打开视图 🖐
- [ ] 左侧栏出现「相机」图标，点击后打开朋友圈视图。
- [ ] 通过命令面板执行「此刻 MOMENT：打开朋友圈视图」也能打开。
- [ ] 再次点击图标或执行命令会聚焦到已打开的视图（不重复开标签页）。

### 1.2 快速发布（右上角 ＋）🖐🔍
- [ ] 点击背景右上角圆形「＋」，弹出发布浮层。
- [ ] 输入文字，敲 `Enter` 直接发送；`Shift+Enter` 换行。
- [ ] 点心情标签可选中/再次点取消选中。
- [ ] 点图片按钮选择 1 张或多张图片，出现缩略预览，可点×移除。
- [ ] 点「发送」，浮层关闭，信息流顶部出现新动态。
- [ ] 空内容点发送会提示「写点什么再发送吧」，不生成文件。

### 1.3 生成文件的文件规范 🔍
发布后，检查「内容根目录」下生成：

- [ ] 正文：`<年-月>/<年-月-日>.md`，例如 `2026-9/2026-9-10.md`。
- [ ] frontmatter 含 `mood` / `quote` / `updated`。
- [ ] 每条动态以 `## 21:04` 开头，配图追加 ` · 附件名`，正文后为 `- 心情：xxx`。
- [ ] 若发了图片：附件目录为 `附件/2026-9/2026-9-10-18:58:30.jpg`（按时间命名，秒级不冲突）。
- [ ] 同一天连续发布，多 `##` 块按时间追加；跨天则生成新日文件。

### 1.4 背景自动加载 🖐
- [ ] 在「附件」目录放入 `img.jpg`，封面立即/下次刷新变为该图。
- [ ] 更换 `img.jpg`，封面随之更新（事件监听）。
- [ ] 删除 `img.jpg`，封面回退为默认渐变。
- [ ] 「设置 → 签名」修改后，右下角签名即时更新。

---

## 2. 氛围（阶段二）

### 2.1 活跃度格点 🖐
- [ ] Git 风格网格：竖向 7 行 = 一周，周一在顶、周六周日在底，星期标签在右侧。
- [ ] 横向为周列，按面板宽度自适应增减列数（拖动加宽/收窄标签页会变）。
- [ ] 最右列为当前周，「今天」定色高亮（深紫+白描边光晕），当前周今天之后的星期留空。
- [ ] 悬停任一格子 → 弹出「日期 · N 条」气泡。
- [ ] 顶部显示「近N天 · 条数」。
- [ ] 发布的当天格子颜色明显加深；有更多动态的格子更接近纯色。
- [ ] 无文件的格子为浅底色（level 0）。

### 2.2 名言分界线 🖐
- [ ] 灰分界线中央随机显示一句名言。
- [ ] 在「设置 → 名言池」增删名言后，刷新后生效；全清空则分界线中央不显示名言。

### 2.3 信息流展示 🖐
- [ ] 列表按「最新在前」排列。
- [ ] 每条仅有时间 + 心情药丸，配图跟随正文下方显示、可点击放大/新开。
- [ ] 今天无动态时显示空状态文案。

---

## 3. 洞察（阶段三）

### 3.1 下拉进入全览 🖐
- [ ] 在信息流滚动到顶后，用鼠标/触控向下拖动，视图整体下拉呈阻尼（越拉越慢）。
- [ ] 拖过约 80px 松手，切换至「全览」相册视图。
- [ ] 拖动不足阈值松手，回弹复位，仍停在朋友圈视图。
- [ ] 操作时遵守系统「减弱动效」（`prefers-reduced-motion`）时收敛为透明度变化。

### 3.2 全览聚合 🖐
- [ ] 顶部有「天 / 周 / 月 / 年」分档，点击切换并重新聚合。
- [ ] 「天」：每个有内容的日期一行，左侧日期，右侧缩略图平铺，文字动态以标记格显示。
- [ ] 「周 / 月 / 年」：按聚合段收成一个卡片，显示「段 · 天数 · 图数」与缩略图。
- [ ] 空跨度显示「这个时间跨度里还没有内容」。
- [ ] 在「全览」顶部再次下拉，退出回朋友圈视图。

---

## 4. 数据同步与健壮性

### 4.1 对外部改动的响应 🔍🖐
- [ ] 用任意编辑器直接修改该日 markdown（增删 `##` 块），回到视图后信息流随之更新。
- [ ] 删除某日文件，刷新后该日动态消失、活跃度格子归零。
- [ ] 后台（另一设备/另一文件）改动同步过来后，进入视图能读到最新内容（轮询 30s 心跳兜底）。

### 4.2 兼容性 / 数据不绑定 🔍
- [ ] 生成的 `.md` 与图片是普通文件：用 Typora / VS Code / 记事本可打开、编辑、删除。
- [ ] 插件禁用后，这些文件依然完好存在。

### 4.3 性能（建议顺带观察）🖐
- [ ] 打开视图首屏信息流快速呈现，无明显卡顿。
- [ ] 信息流在单日条目较多时滚动流畅（虚拟列表只渲染可视区）。
- [ ] 切换 天/周/月/年 聚合无明显卡顿（按需读取）。

---

## 5. 已知边界 / 说明（非缺陷）

- 内容根目录目前按**库内相对路径**对待；跨库的**外部绝对路径**暂未开放（v0.1 范围）。
- 发布配图使用 `Vault.createBinary` 写入，须位于库内。
- 周粒度聚合以「周一起点 + ISO 周号」标识，供人工核对。
- 动效默认 200–260ms；减弱动效模式下收敛为透明度切换。

## 6. 回归重点（改动后必测）
发布→读回、背景更换、下拉打开/关闭全览、聚合档位切换、外部改文件后刷新。